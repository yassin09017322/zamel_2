class AppConfig {
  // Cloudinary configuration
  static const String cloudinaryCloudName = 'ggvpugii';
  static const String cloudinaryUploadPreset = 'yassin_upload';

  // Email handling: Firebase Authentication handles verification and
  // password reset emails. EmailJS integration removed — use Firebase
  // console templates and ActionCodeSettings if custom links are needed.
}
